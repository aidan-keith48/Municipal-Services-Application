<Window
    x:Class="Municipal_Services_Application.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Municipal_Services_Application"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Municipal_Services_Application.ViewModels"
    Title="MainWindow" Width="800" Height="450"
    mc:Ignorable="d">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation Buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Navigate to Report Issues" Command="{Binding NavigateCommand}" CommandParameter="ReportIssues"/>
            <Button Content="Navigate to Local Events" Command="{Binding NavigateCommand}" CommandParameter="LocalEvents"/>
            <Button Content="Navigate to Service Request Status" Command="{Binding NavigateCommand}" CommandParameter="ServiceRequestStatus"/>
        </StackPanel>

        <!-- Content Display Area -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
