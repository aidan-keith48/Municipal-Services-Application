<UserControl x:Class="Municipal_Services_Application.Views.ReportIssuesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Municipal_Services_Application.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" />
        <ComboBox SelectedItem="{Binding Category, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
        <Button Content="Submit" Command="{Binding SubmitCommand}" />
    </StackPanel>
</UserControl>
